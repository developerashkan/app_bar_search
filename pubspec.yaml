name: app_bar_search
description: A lightweight Flutter AppBar widget with integrated search and smooth animations.
version: 1.2.0
homepage: https://github.com/ashkanbmn/app_bar_search

environment:
  sdk: "^3.8.0"
  flutter: "^3.32.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true